// Standalone script to generate and print the VK hex
// Run with: rustc gen_vk.rs --edition 2021 && ./gen_vk

use std::process;

fn main() {
    println!("Use: cargo test print_vk_hex -- --nocapture");
    // We'll use a simpler approach - just print the hex from a test
}
